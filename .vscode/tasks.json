{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Viviore",
            "type": "shell",
            // "command": "g++ main.cpp -lraylib -lGL -lm -lpthread -ldl -lrt -lX11 -o main && ./main",
            "command": "g++ $(find . -name '*.cpp') -lraylib -lGL -lm -lpthread -ldl -lrt -lX11 -o viviore && ./viviore",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
